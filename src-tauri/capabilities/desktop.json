{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "desktop",
  "description": "Desktop-only capabilities (opener plugin)",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "opener:default"
  ]
}
